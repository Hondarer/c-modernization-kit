# ToDo

## gtest のライブラリ

- /MD, /MT, /MDd, /MTd の対象ごとの LIB フォルダは、処理で切り分ける必要あり。
  → もしかしたら release 固定でいいのでは。

## Windows gtest のデバッグ情報

そのときのソースパスが埋め込まれるのでどうかと思う。

## Windows の CI 設計

セットアップされているべきパッケージはわかっているので、頑張ればできる。

## テストフレームワークサンプルの移行
