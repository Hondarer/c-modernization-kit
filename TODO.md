# ToDo

## gtest のライブラリ

- /MD, /MT, /MDd, /MTd の対象ごとの LIB フォルダは、処理で切り分ける必要あり。
  → もしかしたら release 固定でいいのでは。

## Windows gtest のデバッグ情報

そのときのソースパスが埋め込まれるのでどうかと思う。

## Make スキップ

指定したいこと

- ビルドのスキップ (SKIP_BUILD=未指定/0/1/BOTH/WINDOWS/LINUX)
- テスト実行のスキップ (SKIP_TEST=未指定/0/1/BOTH/WINDOWS/LINUX)

を、プラットフォームごとに指定できるようにしたい。  
プラットフォームごとの判定必要か? という点は要審議

## テストの効率化

現在、テストを 2 回通り行っているが、処理を見直せば 1 回で済みそう。
Linux のカバレッジデータも、毎回 xml を累積すれば Windows と同じように処理できるため。
