# メッセージの組み立て (format) 機能仕様

## 概要

メッセージ組み立て機能は、メッセージ ID と利用側が並べた値から、現在の出力言語のメッセージを組み立てます。  
語順は言語別の書式が決め、値の文字列表現はメッセージ ID の引数スキーマが決めます。

## 解決する課題

書式指定を含む文字列をそのまま翻訳の対象にすると、語順の変更に伴って値の並び順も変わります。  
翻訳された書式と、呼び出し側が並べた値の型が食い違うと、出力の誤りでは済まず、実行が壊れます。

値を順番にしか取り出せない仕組みでは、書式の側から取り出す順序を変えられません。  
書式が語順を決める方式のままでは、並べ替えも、同じ値の繰り返しの参照もできません。

書式に値の表示形式を書けるようにすると、言語ごとに表現が変わります。  
同じ値が日本語では 16 進数、英語では 10 進数で記録されると、記録の解析が言語に依存します。

メッセージ組み立て機能は、値の並び順と表現をメッセージ ID の側へ固定し、書式には語順だけを残します。

## 機能要件

| 要件 ID | message_catalog の要件 |
|---|---|
| `MSGCAT-FORMAT-FUNC-001` <!-- msgcat-req: uuid=d6e0fb17-22ba-44d1-9089-b9c8a4b96816 --> | message_catalog のメッセージ組み立て機能は、メッセージ ID と利用側が並べた値から、現在の出力言語の書式に従うメッセージを組み立て、利用側が指定した領域へ格納します。 |
| `MSGCAT-FORMAT-FUNC-002` <!-- msgcat-req: uuid=31e44ef6-0f8c-456b-a846-483b8545b42a --> | message_catalog のメッセージ組み立て機能は、書式中の位置指定を、その添字に対応する値の文字列表現へ置き換えます。 |
| `MSGCAT-FORMAT-FUNC-003` <!-- msgcat-req: uuid=2f8d9f36-2f08-4098-940b-dc0efce1f83c --> | message_catalog のメッセージ組み立て機能は、位置指定が書式に現れる順序に依らず、利用側が並べる値の順序を引数スキーマが定める順序のままとします。 |
| `MSGCAT-FORMAT-FUNC-004` <!-- msgcat-req: uuid=873645f7-d29b-4440-b253-a569c1467532 --> | message_catalog のメッセージ組み立て機能は、同じ添字の位置指定が書式に複数回現れる場合、同じ値をその回数だけ展開します。 |
| `MSGCAT-FORMAT-FUNC-005` <!-- msgcat-req: uuid=f68670a7-9a79-4261-973e-2c5b0083bd28 --> | message_catalog のメッセージ組み立て機能は、書式中の連続する 2 文字の中かっこを、1 文字の中かっことして出力します。 |
| `MSGCAT-FORMAT-FUNC-006` <!-- msgcat-req: uuid=593bcc1b-265a-4b77-af09-18fb71a831c6 --> | message_catalog のメッセージ組み立て機能は、値の文字列表現を、メッセージ ID の引数スキーマが定める引数種別に従って決めます。 |
| `MSGCAT-FORMAT-FUNC-007` <!-- msgcat-req: uuid=2add9ca8-74e7-4a76-a78b-e335fe6a448f --> | message_catalog のメッセージ組み立て機能は、組み立てた結果が格納先に収まらない場合、収まる範囲まで格納し、切り詰めたことを利用側へ通知します。 |
| `MSGCAT-FORMAT-FUNC-008` <!-- msgcat-req: uuid=054c115c-c713-471b-a221-dd8028f76f37 --> | message_catalog のメッセージ組み立て機能は、未登録のメッセージ ID を指定された場合、組み立てを行わず、未登録であることを利用側へ通知します。 |
| `MSGCAT-FORMAT-FUNC-009` <!-- msgcat-req: uuid=2d675edd-afbf-4a33-8d5f-7d3ded23d640 --> | message_catalog のメッセージ組み立て機能は、書式の構文が不正な場合、または位置指定の添字が引数の個数を超える場合、定義が不正であることを利用側へ通知します。 |
| `MSGCAT-FORMAT-QUAL-001` <!-- msgcat-req: uuid=65754b28-ca15-471b-a8a2-14fa9e128e15 --> | message_catalog のメッセージ組み立て機能は、組み立ての成否に依らず、格納先を終端された文字列にします。 |
| `MSGCAT-FORMAT-QUAL-002` <!-- msgcat-req: uuid=f6ee0432-a4c9-4584-b42d-73dc7990fe0b --> | message_catalog のメッセージ組み立て機能は、利用側が指定した容量を超えて格納先へ書き込みません。 |
| `MSGCAT-FORMAT-QUAL-003` <!-- msgcat-req: uuid=0a2a9154-d08a-4c96-a509-335fea89b0ab --> | message_catalog のメッセージ組み立て機能は、出力言語とカタログを変更しない限り、複数のスレッドが同時に組み立てても、それぞれの結果を互いに変化させません。 |
| `MSGCAT-FORMAT-COMP-001` <!-- msgcat-req: uuid=0466e82e-ca3d-4046-9a05-af3eb80f25f3 --> | message_catalog のメッセージ組み立て機能は、同じ値と同じ引数種別に対し、対応するプラットフォーム間で同じ文字列表現を出力します。 |
| `MSGCAT-FORMAT-COMP-002` <!-- msgcat-req: uuid=4c240165-7856-4854-bb71-d701ed0d6b62 --> | message_catalog のメッセージ組み立て機能は、組み立てた結果を UTF-8 で出力します。 |
| `MSGCAT-FORMAT-CONS-001` <!-- msgcat-req: uuid=d9c0bcba-09b6-4f6e-b2ba-ab892d95ff10 --> | message_catalog のメッセージ組み立て機能は、書式が解釈する構文を位置指定とエスケープだけとし、値の表示形式を指定する記法を持ちません。 |
| `MSGCAT-FORMAT-CONS-002` <!-- msgcat-req: uuid=4a527b64-2e5f-426b-a196-0acea485e566 --> | message_catalog のメッセージ組み立て機能は、1 つのメッセージが取る引数の個数を 10 個以下とします。 |
| `MSGCAT-FORMAT-CONS-003` <!-- msgcat-req: uuid=6781aead-aa20-46d3-94c8-227d96ddb356 --> | message_catalog のメッセージ組み立て機能は、値を受け取る型を、可変長引数の既定の型変換のあとの型と一致させます。 |
| `MSGCAT-FORMAT-CONS-004` <!-- msgcat-req: uuid=b0f4a1c7-6d52-4e88-9a31-7c5e2f0d4b96 --> | message_catalog のメッセージ組み立て機能は、値を受け取る型を、対応するプラットフォームで同じ幅を持つ型だけで構成します。 |

## 値の並び順と語順

利用側は、メッセージ ID の引数スキーマが定める順序で値を並べます。  
語順が異なる言語へ切り替えても、この順序は変わりません。

書式は、位置指定の添字によって値を選びます。  
そのため、書式の側では値の並べ替えと繰り返しの参照ができます。

```text
引数スキーマ    : 0 番目 = ファイル名、1 番目 = エラー コード
利用側が並べる値: "config.json", 2
ニュートラル言語: "Failed to open file {0}. Error code={1}"
日本語          : "ファイル {0} を開けませんでした。エラー コード={1}"
```

## 書式の構文

書式が解釈するのは、位置指定とエスケープだけです。

| 記法 | 意味 |
|---|---|
| `{0}` から `{9}` | 添字に対応する値を、引数種別が定める表現で展開します。 |
| `{{` | `{` を 1 文字出力します。 |
| `}}` | `}` を 1 文字出力します。 |

添字は 1 桁だけを受け付けます。  
引数の個数の上限が 10 個であるのは、1 桁の添字で表せる範囲がここまでであるためです。

2 桁以上の添字、数字以外の添字、引数の個数を超える添字、対を成さない中かっこは、いずれも定義の誤りとして扱います。  
表示形式を指定する記法を持たないのは、同じ値の表現が言語によって変わることを防ぐためです。

## 値の文字列表現

引数種別は、値として受け取る型と文字列表現を 1 つに定めます。  
型と表示形式を別々に選べるようにすると、文字列を 16 進数で表すような、意味を持たない組み合わせを表現できてしまいます。

| 引数種別 | 文字列表現 | 例 |
|---|---|---|
| 文字列 | 受け取った文字列をそのまま出力します。値が無い場合は `(null)` を出力します。 | `config.json` |
| 文字 | 印字できる場合は単引用符で囲んだ 1 文字。印字できない場合は 10 進数と 16 進数の併記 | `'A'` / `138 (0x8a)` |
| 符号付き 8 bit 整数 | 10 進数 | `-12` |
| 符号なし 8 bit 整数 | 10 進数 | `200` |
| 符号付き 16 bit 整数 | 10 進数 | `-1200` |
| 符号なし 16 bit 整数 | 10 進数 | `48000` |
| 符号付き 32 bit 整数 | 10 進数 | `-12` |
| 符号なし 32 bit 整数 | 10 進数 | `12` |
| 符号付き 64 bit 整数 | 10 進数 | `-4294967296` |
| 符号なし 64 bit 整数 | 10 進数 | `4294967296` |
| 8 bit の 16 進数 | 2 桁の 16 進数 | `0x8a` |
| 16 bit の 16 進数 | 4 桁の 16 進数 | `0xbeef` |
| 32 bit の 16 進数 | 8 桁の 16 進数 | `0x1234abcd` |
| 64 bit の 16 進数 | 16 桁の 16 進数 | `0x00000000deadbeef` |
| 要素数またはバイト数 | 10 進数 | `4096` |
| 符号付きの要素数またはバイト数 | 10 進数 | `-1` |
| アドレス | アドレスの幅に合わせた桁数の 16 進数 | `0x00007fffa1234567` |
| 倍精度浮動小数点数 | 有効な桁を保つ短い表現 | `12.5` |
| エラー コード | 10 進数と 16 進数の併記 | `2 (0x00000002)` |

16 進数の桁数は引数種別ごとに固定し、英小文字で出力します。  
エラー コードは、Linux の `errno` と Windows のエラー コードのどちらも同じ整数として受け取り、値の変換や説明文の付加を行いません。

8 bit と 16 bit の整数は、可変長引数の既定の型変換によってより大きい型へ変換されます。  
これらの引数種別は、変換後の型で値を受け取ったあと、種別が表す幅へ戻してから表現を決めます。  
変換後の型と一致しない受け取り方をすると、利用側が値を渡した時点で実行が壊れます。

単精度浮動小数点数の引数種別を設けていないのは、変換後の倍精度として扱えば足りるためです。

文字の引数種別は、印字できるかどうかをロケールに依らず判定します。  
判定をロケールに委ねると、同じ値の表現が実行環境によって変わります。

符号付きの要素数またはバイト数は、幅が処理系で異なる型を避け、64 bit の符号付き整数として受け取ります。  
対応するプラットフォームのどちらでも同じ幅になり、値の範囲が実行環境によって変わりません。

## 失敗時の振る舞い

| 条件 | 格納先 | 利用側への通知 |
|---|---|---|
| 格納先の指定が無い、または容量が 0 | 変更しません | 引数が不正であること |
| メッセージ ID が未登録 | 終端された空の文字列にします | 未登録であること |
| 書式の構文または添字が不正 | 終端された文字列にします | 定義が不正であること |
| 結果が容量に収まらない | 収まる範囲まで格納します | 切り詰めたこと |

切り詰めた結果も終端された文字列であり、利用側はそのまま出力できます。  
切り詰めは組み立ての失敗であり、記録の欠落として扱えます。

## プラットフォーム間の差

アドレスの表現は、環境が定める既定の表記ではなく、アドレスの幅に合わせた桁数の 16 進数へ揃えます。  
これにより、Linux と Windows で同じ値のアドレスが同じ文字列になります。

出力は UTF-8 です。  
端末が UTF-8 以外の符号化を使う場合、文字化けの回避は利用側が行います。

## 適用範囲外

- 組み立てたメッセージの出力先への書き出しと、分類値による絞り込み
- 複数形、性、数による語形の選択
- 桁区切り、小数点、日時の表記など、地域の慣習に依存する整形
- 値ごとの表示形式を書式で指定する記法
- 10 個を超える引数
- 時刻やプロセス ID など、組み立て機能が自ら収集する値
- 端末や出力先の符号化の設定

## 前提と制約

利用側は、メッセージ ID の引数スキーマが定める順序と型で値を並べます。  
異なる型の値を渡した場合、組み立ての結果は保証しません。

利用側は、格納先の領域と容量を用意します。  
必要な容量は、書式と値の表現から利用側が見積もります。

書式と引数スキーマの整合は、カタログの点検で確認します。  
組み立て時に確認するのは、書式の構文と、位置指定の添字が引数の個数に収まることだけです。

出力言語の書式が欠けているメッセージでは、ニュートラル言語の書式を使用します。

## 選択の指針

値の意味に合わせて引数種別を選びます。  
同じ整数でも、桁数を固定した 16 進数で記録したい識別子と、10 進数で読みたい件数では、選ぶ種別が異なります。

OS が返す誤りの番号には、エラー コードの種別を選びます。  
10 進数と 16 進数を併記するため、Linux の記録と Windows の記録を同じ手順で読み解けます。

読み取ったバイト数のように、負の値で失敗を表す数量には、符号付きの要素数またはバイト数の種別を選びます。  
1 バイトの値をそのまま示したい場合は文字の種別を、ビットの並びとして読みたい場合は 8 bit の 16 進数の種別を選びます。

## 関連する機能仕様と外部仕様

- [カタログ (catalog) 機能仕様](catalog.md)
- [言語 (language) 機能仕様](language.md)
