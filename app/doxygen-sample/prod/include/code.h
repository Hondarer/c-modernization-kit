/**
 *  @brief          文字列を反転します。
 *  @param[in,out]  str 反転する文字列。
 *  @details        以下は使用例です。
    @code{.c}
    char text[] = "Hello";
    reverseString(text);
    printf("%s\n", text);  // 出力: olleH
    @endcode
 */
void reverseString(char *str);
